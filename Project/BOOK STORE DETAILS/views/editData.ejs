<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background-color: #f9f9f9;
            color: #333;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }

        form {
            margin-bottom: 20px;
            padding: 20px;
            background-color: #ffffff;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            width: 40%;
            border-collapse: collapse;
            margin: auto;
        }

        form table td {
            padding: 10px;
            vertical-align: middle;
        }

        form input[type="text"],
        form select {
            width: 100%;
            padding: 8px;
            margin: 5px 0;
            border: 1px solid #ddd;
            border-radius: 4px;
            box-sizing: border-box;
        }

        form input[type="radio"] {
            margin-right: 5px;
        }

        form button {
            background-color: #28a745;
            color: #ffffff;
            border: none;
            padding: 10px 15px;
            cursor: pointer;
            border-radius: 4px;
            font-size: 16px;
        }

        form button:hover {
            background-color: #218838;
        }
    </style>
</head>

<body>
    <form action="/updateData/<%= storeData.id %>" method="post">
        <table>
            <tr>
                <td><label for="">Book Name :</label></td>
                <td> <input type="text" placeholder="Enter Book Name" value="<%= storeData.bookname %>" name="bookname"></td>
            </tr>
            <tr>
                <td> <label for="">Price :</label></td>
                <td><input type="text" placeholder="Enter Book Price" value="<%= storeData.price %>"  name="price"></td>
            </tr>
            <tr>
                <td><label for="">Section : </label></td>
                <td>
                    <input type="radio" name="section" value="A" <%= storeData.section.includes("A") ? "checked" : "" %> />A
                    <input type="radio" name="section" value="B" <%= storeData.section.includes("B") ? "checked" : "" %>/>B
                </td>
            </tr>
            <tr>
                <td>Category</td>
                <td>
                    <select name="Category" id="">
                        <option value="">Select Category</option>
                        <option value="Science fiction"  <%= storeData.Category === "Science fiction" ? "selected" : "" %>>Science fiction</option>
                        <option value="Adventure fiction"<%= storeData.Category === "Adventure fiction" ? "selected" : "" %>>Adventure fiction</option>
                        <option value="Autobiography"    <%= storeData.Category === "Autobiography" ? "selected" : "" %>>Autobiography</option>
                        <option value="Mystery"  <%= storeData.Category === "Mystery" ? "selected" : "" %>>Mystery</option>
                        <option value="Thriller" <%= storeData.Category === "Thriller" ? "selected" : "" %>>Thriller</option>
                    </select>
                </td>
            </tr>

            <tr>
                <td><button type="submit">Update Data</button></td>
            </tr>
        </table>
    </form>
</body>
</html>